---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={['block text-[0.72rem] font-bold uppercase tracking-[0.2em] text-accent', className]}>
  <slot />
</span>

